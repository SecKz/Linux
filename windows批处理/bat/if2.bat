::就需要引入流程跳转的概念，动用流程跳转语句 goto 了。流程跳转的含义是：改变默认的执行顺序，强制跳转到指定的位置执行特定的程序块。

@echo off
color 0A

:先清空字符串
set str=

set /p str=请输入字母A或B：
if "%str%"=="A" (echo 是A) else echo "不是A"

echo ----------------------------------------------------------

if %str%=="" (
echo 输入的字符串为空
echo %date%
)

echo ----------------------------------------------------------

:not的应用

if not 1==2 (
echo 这是not的用法
echo 当前批处理全路径：%~f0
echo 当前CMD默认目录：%cd%
)

