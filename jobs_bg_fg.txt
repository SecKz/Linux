

恢复进程执行时，有两种选择：fg命令将挂起的作业放回到前台执行；用bg命令将挂起的作业放到后台执行

# jobs 					查看当前有多少在后台运行的命令		相当于bg
# command & 			让进程在后台运行 
# fg %n 					让后台运行的进程n到前台来执行 
# bg %n 			让进程n到后台去，将一个在后台暂停的命令，变成继续执行   
# ctrl + z 			可以将一个正在前台执行的命令放到后台，并且暂停
# fg 				将后台中的命令调至前台继续运行
# kill %1				关掉第一个job

关掉后台作业
方法一：通过jobs命令查看job号（假设为num），然后执行kill %num
方法二：通过ps命令查看job的进程号（PID，假设为pid），然后执行kill pid
# pstree -up | grep mysql











