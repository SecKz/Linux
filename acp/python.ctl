#TITLE=Python常用
#SORT=y

#T=from pprint import pprint as pp
from pprint import pprint as pp
#T=os.mkdir
os.path.exists(upload_dir) or os.mkdir(upload_dir)
#T=计算耗时
start = time.time()
print ("time: %.3fs" % (end - start))
#